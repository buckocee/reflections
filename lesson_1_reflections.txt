Q:How did viewing a diff between two versions of a file help you see the bug that was introduced?
A: diff made it easier to identify the bug by:
	-eliminating unchanged lines from the results displayed
	-annotating the lines of code that changed

Q: How could having easy access to the entire history of a file make you a more efficient programmer in the long term?
A: It makes it easier to undo changes  and revert to a previous state

Q: What happens when you initialize a repository? Why do you need to do it?
A: Git creates a .git folder with the repository metadata
